{
  "name": "fucare-express-rollback",
  "version": "1.0.0",
  "private": true,
  "description": "FuCare legacy Express rollback implementation; use the root Python FastAPI service for active development.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "legacy:dev": "nodemon src/server.js",
    "legacy:start": "node src/server.js",
    "legacy:test": "vitest run",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "member3:dev": "cd backend-member3 && python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 3000",
    "member5:dev": "cd backend-member5 && python -m uvicorn main:app --reload --port 8000",
    "legacy:test:watch": "vitest",
    "db:init": "python database/init_sqlite.py",
    "db:test": "python database/test_database_usage.py"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "better-sqlite3": "^12.4.1",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "supertest": "^7.1.4",
    "vitest": "^3.2.4"
  }
}
