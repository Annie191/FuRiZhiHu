# 《伏日智护（FuCare）》技术文档

## 一、项目概述

### 1.1 项目背景

随着夏季高温天气和“三伏天”环境对人体健康影响日益明显，饮水不足、作息紊乱、运动安排不合理、饮食不均衡等问题变得更加突出。针对这一现实场景，我们设计并实现了“伏日智护（FuCare）”健康管理 Web 应用，希望为用户提供更贴近高温季节需求的健康管理服务。

### 1.2 项目目标

本项目以“高温天气下的个人健康管理”为核心，围绕用户画像、健康记录、数据分析和养生建议四个方向展开，实现以下目标：

- 提供用户注册、登录和个人健康档案管理功能
- 支持饮水、饮食、运动、睡眠等日常健康行为记录
- 结合天气与健康数据生成健康概览、趋势分析和健康报告
- 根据规则生成适合夏季和三伏天场景的养生建议与每日计划
- 构建一个适合课程展示、可实际运行的完整 Web 项目

### 1.3 项目定位

伏日智护是一款面向夏季高温场景的健康管理平台。它不仅关注常规的行为记录，还强调天气环境与健康管理之间的联系，使系统更具场景化、实用性和展示价值。

## 二、系统总体架构

本项目采用“前端 + 多后端模块 + 共享数据库”的协作式架构。

```text
伏日智护系统
├─ 前端展示层（React + Vite）
│  ├─ 登录注册
│  ├─ 健康首页 Dashboard
│  ├─ 健康画像
│  ├─ 饮水/饮食/运动/睡眠记录
│  ├─ 每日计划
│  ├─ 每日/每周报告
│  ├─ 趋势分析
│  ├─ 天气环境
│  └─ 社区互动
│
├─ 后端业务层（成员 3：FastAPI）
│  ├─ 用户注册登录
│  ├─ JWT 身份认证
│  ├─ 健康档案接口
│  ├─ 食物库接口
│  └─ 四类健康记录 CRUD 接口
│
├─ 推荐规则层（成员 4：Python 规则引擎）
│  ├─ 健康评分计算
│  ├─ 天气风险处理
│  ├─ 三伏养生规则判断
│  └─ 每日计划生成
│
├─ 分析服务层（成员 5：FastAPI）
│  ├─ Dashboard 综合分析
│  ├─ 每日健康报告
│  ├─ 每周健康报告
│  ├─ 健康指数趋势分析
│  └─ 综合趋势总结
│
└─ 数据层（SQLite）
   ├─ 用户表
   ├─ 用户画像表
   ├─ 饮水/饮食/运动/睡眠记录表
   ├─ 食物库表
   ├─ 天气表
   ├─ 计划表
   └─ 社区动态表
```

## 三、技术选型

### 3.1 前端技术

- React 18：构建单页应用页面
- Vite：前端开发与构建工具
- React Router DOM：实现页面路由管理
- Recharts：实现图表可视化展示
- Lucide React：提供页面图标
- CSS：完成页面布局与样式设计

### 3.2 后端技术

- Python FastAPI：实现主要业务接口与分析接口
- JWT：实现用户登录认证
- bcrypt：实现密码加密存储
- NumPy：实现趋势分析中的数值计算

### 3.3 数据库技术

- SQLite：作为项目统一数据存储方案
- SQL 视图：用于健康汇总与 Dashboard 聚合
- 触发器与约束：用于保证数据库结构完整性

### 3.4 保留模块

项目根目录下保留了基于 Node.js + Express 的历史版本回退实现，主要用于兼容和参考；当前正式使用的是 Python FastAPI 服务。

## 四、系统功能设计

### 4.1 用户模块

用户模块负责账号管理和身份认证，主要包括：

- 用户注册
- 用户登录
- 获取当前登录用户信息
- JWT 鉴权

注册时系统会同时创建用户账号和健康画像信息，为后续健康管理功能提供基础数据支撑。

### 4.2 健康画像模块

健康画像模块用于描述用户的基础健康状态和目标，包括：

- 年龄
- 性别
- 身高
- 体重
- BMI
- 健康目标
- 运动水平
- 平均睡眠时长
- 饮水目标
- 用户画像标签

该模块既用于前端展示，也作为推荐和分析的重要输入。

### 4.3 健康记录模块

健康记录模块包括四类核心记录：

#### 4.3.1 饮水记录

- 记录饮水量
- 记录饮水来源
- 记录饮水时间
- 统计每日饮水总量与次数

#### 4.3.2 饮食记录

- 选择食物库中的食物
- 记录餐次与摄入量
- 统计热量、蛋白质和水分
- 生成饮食健康评分

#### 4.3.3 运动记录

- 记录运动类型
- 记录强度、时长与热量消耗
- 支持按日统计运动情况

#### 4.3.4 睡眠记录

- 记录入睡与起床时间
- 记录睡眠质量评分
- 统计睡眠时长和睡眠质量

### 4.4 Dashboard 健康首页

Dashboard 是系统最核心的页面，主要展示：

- 当前用户健康概览
- 今日健康指数
- 饮水、运动、饮食、睡眠完成情况
- 天气与环境信息
- 今日养生计划
- 健康趋势摘要

它将多个模块的数据集中展示，帮助用户快速了解“今天的健康状态”。

### 4.5 每日养生计划模块

该模块结合以下数据自动生成建议：

- 用户画像
- 当日健康汇总数据
- 天气环境信息
- 三伏天规则

生成内容主要分为四个时间段：

- 早餐建议
- 午间建议
- 运动建议
- 晚间建议

### 4.6 报告与趋势分析模块

#### 每日健康报告

展示当天的健康评分与四个维度的完成情况，包括：

- 饮水完成度
- 运动完成度
- 饮食评分
- 睡眠质量

#### 每周健康报告

统计一段时间内的健康变化，并生成总结性描述，便于用户了解一周整体状态。

#### 趋势分析

通过折线图、柱状图等方式展示：

- 健康指数变化趋势
- 饮水、运动、睡眠等行为变化趋势
- 综合趋势总结

### 4.7 天气与社区模块

#### 天气模块

展示指定城市在一段时间内的：

- 温度
- 湿度
- 紫外线指数
- 空气质量指数
- 高温风险等级
- 健康提示

#### 社区模块

用于展示公开健康动态，包括：

- 用户发布内容
- 发布时间
- 点赞数量

## 五、数据库设计

### 5.1 核心表结构

项目数据库位于 `database/` 目录，核心数据表如下：

| 表名 | 说明 |
| --- | --- |
| `user_account` | 用户账号表 |
| `user_profile` | 用户健康画像表 |
| `food_library` | 食物库表 |
| `food_record` | 饮食记录表 |
| `water_record` | 饮水记录表 |
| `sport_record` | 运动记录表 |
| `sleep_record` | 睡眠记录表 |
| `weather_daily` | 天气与环境信息表 |
| `daily_plan` | 每日养生计划表 |
| `community_post` | 社区动态表 |

### 5.2 汇总视图

为了方便前端展示和分析模块调用，数据库额外设计了多个汇总视图：

- `v_daily_food_summary`
- `v_daily_water_summary`
- `v_daily_sport_summary`
- `v_daily_sleep_summary`
- `v_dashboard_daily`

这些视图使得后端可以直接读取聚合数据，提高了分析类接口的实现效率。

## 六、核心业务逻辑

### 6.1 健康评分逻辑

系统健康指数采用加权评分方式：

```text
健康指数 =
饮水完成度 × 30%
+ 运动完成度 × 25%
+ 饮食健康度 × 25%
+ 睡眠质量 × 20%
```

其中：

- 饮水完成度 = 实际饮水量 / 饮水目标
- 运动完成度 = 实际运动时长 / 目标运动时长
- 饮食健康度 = 当日饮食评分
- 睡眠质量 = 当日睡眠评分

该评分逻辑既简单直观，又便于与页面图表展示结合。

### 6.2 推荐规则逻辑

成员 4 模块使用规则引擎来生成每日建议，主要考虑：

- 高温天气风险
- 三伏天时段
- 用户健康目标
- 用户当日行为表现

这种方式实现难度适中，且适合课程项目展示“规则推荐”的设计思想。

### 6.3 趋势分析逻辑

成员 5 模块基于数据库中的汇总数据进行统计分析：

- 计算健康指数变化趋势
- 统计饮水、运动、睡眠等均值
- 生成趋势描述和综合评语

该模块体现了项目从“记录型系统”向“分析型系统”的扩展能力。

## 七、接口设计概述

### 7.1 成员 3 核心业务接口

- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`
- `GET /api/v1/auth/me`
- `GET /api/v1/profile`
- `PATCH /api/v1/profile`
- `GET /api/v1/foods`
- `GET /api/v1/foods/{food_id}`
- `GET/POST /api/v1/water-records`
- `GET/PATCH/DELETE /api/v1/water-records/{record_id}`
- `GET /api/v1/water-records/daily-summary`
- `food-records / sport-records / sleep-records` 与之结构一致

### 7.2 成员 5 分析接口

- `GET /api/dashboard/users`
- `GET /api/dashboard/{user_id}`
- `GET /api/dashboard/{user_id}/series`
- `GET /api/report/{user_id}/daily`
- `GET /api/report/{user_id}/weekly`
- `GET /api/statistics/{user_id}/overview`
- `GET /api/trend/{user_id}/health-score`
- `GET /api/trend/{user_id}/summary`
- `GET /api/plans/{user_id}`
- `GET /api/weather`
- `GET /api/community/posts`

## 八、前后端协作方式

### 8.1 数据流说明

系统的数据流大致如下：

1. 用户在前端注册或登录
2. 成员 3 后端返回 JWT Token
3. 前端在记录类页面调用成员 3 接口
4. 前端在报告、趋势、天气、社区页面调用成员 5 接口
5. 成员 4 推荐逻辑基于数据库与接口数据生成计划
6. 所有模块共享统一 SQLite 数据库

### 8.2 前端代理配置

在 `frontend/vite.config.js` 中设置了开发代理：

- `/member3` 转发到 `127.0.0.1:3000`
- `/member5` 转发到 `127.0.0.1:8000`

这样前端在本地开发时可以通过统一相对路径访问两个后端模块。

## 九、项目运行方式

### 9.1 推荐启动顺序

1. 初始化数据库
2. 启动成员 3 后端
3. 启动成员 5 后端
4. 启动前端页面

### 9.2 参考命令

```powershell
python database/init_sqlite.py

cd backend-member3
python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 3000

cd ..\backend-member5
python -m uvicorn main:app --reload --port 8000

cd ..\frontend
npm run dev
```

## 十、测试与验证说明

项目中已包含如下测试或验证资源：

- `tests/`：根目录回退版服务测试
- `backend-member3/tests/`：成员 3 后端测试
- `backend_member4/tests/`：成员 4 规则引擎测试
- `database/test_database_usage.py`：数据库使用验证

由于当前环境未安装完整依赖，本次主要依据代码结构、模块设计、接口实现和静态逻辑进行审查与总结。整体来看，项目结构清晰，模块边界明确，已经具备较完整的课程项目展示条件。

## 十一、项目特色与成果总结

本项目具有以下几个比较突出的特点：

- 主题明确，聚焦“高温天气与三伏天健康管理”
- 前后端分工清晰，团队协作结构完整
- 功能覆盖记录、展示、分析、推荐多个层次
- 使用共享数据库和汇总视图支撑多模块联动
- 兼顾课程项目难度与展示效果，适合答辩演示

总体而言，伏日智护项目完成了从需求设计、数据库设计、前端实现、后端开发到分析展示的完整流程，是一个具有较强综合性的 Web 应用课程项目。

